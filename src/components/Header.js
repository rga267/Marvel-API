import React from "react";
import logo from "../img/logo.png";

const Header = () => {
  return (
    <header className="center">
      <img src={logo} alt="marvel comics logo" />
    </header>
  );
};

export default Header;
